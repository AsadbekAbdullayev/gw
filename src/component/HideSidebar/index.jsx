import React from 'react';
// import { useNavigate } from 'react-router-dom';
import { Container } from './style';
const HideSidebar = ({ open }) => {
  return <Container open={open}>HideSidebar</Container>;
};

export default HideSidebar;
